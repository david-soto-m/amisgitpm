use git2::Repository;
use crate::dbmanager::Proyect;

pub fn config_repo(_repo: Repository)-> Proyect{
    todo!()
}




#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}
